{
  "name": "urussu-frontend",
  "version": "0.8.0",
  "flat": true,
  "contributors": [
    "The Polymer Authors"
  ],
  "license": "BSD-3-Clause",
  "scripts": {
    "start": "node ./env.js && polymer serve",
    "build": "npm run build:prpl-server && npm run build:static",
    "build:static": "node ./env.js && polymer build",
    "serve:static": "polymer serve --port 5000 build/esm-bundled"
  },
  "dependencies": {
    "@polymer/lit-element": "0.6.0-dev.5",
    "@webcomponents/webcomponentsjs": "^2.0.0",
    "leaflet": "^1.3.1",
    "pwa-helpers": "^0.8.2",
    "redux": "^4.0.0",
    "redux-thunk": "^2.3.0",
    "reselect": "^3.0.1"
  },
  "devDependencies": {
    "polymer-cli": "^1.7.6",
    "polyserve": "^0.27.0",
    "renamer": "^0.6.1",
    "replace": "^1.0.0"
  }
}
